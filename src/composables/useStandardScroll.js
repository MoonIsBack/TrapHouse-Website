// Ursprüngliches Standardscrollen:
// Es werden absichtlich keine Events abgefangen und keine Klassen gesetzt.
// Browser, Touchpad, Mausrad und Touchscreen verhalten sich vollständig nativ.
export function useStandardScroll() {
  // Durch dieselbe Schnittstelle kann diese leere Standardvariante in
  // useHomeScroll genau wie der Kapitelmodus ausgewählt werden.
}
